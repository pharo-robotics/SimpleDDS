"
This error is typed because it can be managed. 

Is spawn by the service caller when the server informs an error. 
"
Class {
	#name : #DDSServiceCallFailed,
	#superclass : #MDDSError,
	#category : 'SimpleDDS-Error'
}
